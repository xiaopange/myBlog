# 依赖目录
node_modules/
# VitePress构建产物
docs/.vitepress/dist/
# VitePress开发服务器缓存
docs/.vitepress/cache
# 包管理锁文件（若你用npm/yarn，保留对应锁文件即可，这里全列出来供选择）
package-lock.json
yarn.lock
pnpm-lock.yaml
# 环境变量文件
.env
.env.local
# 编辑器配置文件（VS Code为例，其他编辑器可按需添加）
.vscode/
# 系统自动生成的临时文件
.DS_Store
Thumbs.db
# 日志文件
*.log
# 项目静态文件
projectFiles/
# trae IDE工作文件
.trae/